# Dragon Dance Academy Frontend

React + Vite + TailwindCSS single-page application for the Dragon Dance Academy platform.

## Getting started

```bash
cd frontend
npm install
npm run dev
```

The development server runs on http://localhost:5173 and proxies API requests to http://localhost:8000.

To build a production bundle:

```bash
npm run build
```
